<!DOCTYPE html>
<html lang="en">
<head>
  <%= render "layouts/head" %>
</head>
<body class="main-content">
  <%= render 'layouts/header' %>
   <div class="container">
      <%= yield %>
      <%= debug(params) if Rails.env.development? %>
   </div>
</body>
</html>
